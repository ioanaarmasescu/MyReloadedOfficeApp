@model IEnumerable<MyReloadedOfficeApp.Models.FloorsModel>

@{
    ViewBag.Title = "IndexError";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@if (ViewBag.Message != null)
{
    <script type="text/javascript">

            window.onload = function () {
                alert("@ViewBag.Message");
            };
    </script>

}

<h2>IndexError</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdFloor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdDepartment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdBuilding)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BookableSeats)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FloorDescription)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdFloor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdDepartment)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdBuilding)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BookableSeats)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FloorDescription)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.IdFloor }) |
                @Html.ActionLink("Details", "Details", new { id = item.IdFloor }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.IdFloor })
            </td>
        </tr>
    }

</table>
